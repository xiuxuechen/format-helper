# 严重程度

| severity | 使用场景 |
| --- | --- |
| `blocker` | 文档不可读、规则缺失、无法生成必要快照 |
| `high` | 自动目录缺失、标题结构无法支撑目录、修复会影响交付 |
| `medium` | 样式不合规、疑似标题未纳入大纲、正文直接格式覆盖 |
| `low` | 局部格式漂移、可安全规范化的轻微问题 |

`confidence` 使用 0 到 1。低于 0.75 的结构判断默认进入人工确认。
